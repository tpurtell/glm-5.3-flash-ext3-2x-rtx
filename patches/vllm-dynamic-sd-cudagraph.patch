diff --git a/vllm/v1/worker/gpu/cudagraph_utils.py b/vllm/v1/worker/gpu/cudagraph_utils.py
--- a/vllm/v1/worker/gpu/cudagraph_utils.py
+++ b/vllm/v1/worker/gpu/cudagraph_utils.py
@@ -108,6 +108,7 @@ class CUDAGraphWrapper:
         decode_query_len: int,
         lora_capture_cases: list[int] | None = None,
         varlen_decode: bool = False,
+        use_dynamic_decode_query_len: bool = True,
     ):
         self.vllm_config = vllm_config
         self.device = device
@@ -117,6 +118,9 @@ class CUDAGraphWrapper:
         self.cudagraph_mode = cudagraph_mode
         self.decode_query_len = decode_query_len
         self.varlen_decode = varlen_decode
+        # Autoregressive draft decode remains one token per request even when
+        # dynamic speculative decoding changes target verification length.
+        self.use_dynamic_decode_query_len = use_dynamic_decode_query_len
-
+
         self.dp_size = vllm_config.parallel_config.data_parallel_size
         self.tp_size = vllm_config.parallel_config.tensor_parallel_size
@@ -195,7 +199,8 @@ class CUDAGraphWrapper:
         # to capture graphs for all possible values during decode.
         speculative_config = self.vllm_config.speculative_config
         if (
-            speculative_config
+            self.use_dynamic_decode_query_len
+            and speculative_config
             and speculative_config.uses_dynamic_speculative_decoding()
         ):
             num_spec_per_batch_size = (
diff --git a/vllm/v1/worker/gpu/spec_decode/autoregressive/speculator.py b/vllm/v1/worker/gpu/spec_decode/autoregressive/speculator.py
--- a/vllm/v1/worker/gpu/spec_decode/autoregressive/speculator.py
+++ b/vllm/v1/worker/gpu/spec_decode/autoregressive/speculator.py
@@ -142,6 +142,9 @@ class AutoregressiveSpeculator:
             self.device,
             cudagraph_mode,
             decode_query_len=1,
+            # Draft decode has fixed query length one; only the target
+            # verifier follows the runtime dynamic-SD schedule.
+            use_dynamic_decode_query_len=False,
         )
-
+
     def capture(self) -> None:
